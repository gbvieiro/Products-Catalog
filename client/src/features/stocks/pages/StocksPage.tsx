/**
 * Stub proposital: o pedido original era ter Orders 100% funcional como
 * referencia e deixar as demais features como placeholder para voce
 * replicar o padrao. Para implementar de verdade, siga exatamente a
 * estrutura de `features/orders/` (types.ts -> api/stocksApi.ts ->
 * hooks/useStocks.ts -> pages/StocksPage.tsx), so que chamando os
 * endpoints GET /api/stocks e PUT /api/stocks/book/{bookId}/add.
 */
export function StocksPage() {
  return (
    <section>
      <h1>Stocks</h1>
      <p>
        Ainda nao implementado nesta tela - use o Swagger da Api
        (<code>/swagger</code>) para gerenciar estoque por enquanto, ou siga o
        padrao de <code>features/orders/</code> para construir esta pagina.
      </p>
    </section>
  )
}
